services:
  p4p-interface:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '5125:3000'
    volumes:
      - ./data:/app/data
    environment:
      - .env
    user: "${UID:-1000}:${GID:-1000}"
    restart: always